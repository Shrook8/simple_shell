#include "simple.h"

/**
 * custom_getline - getline
 * @command_buff: command
 * @n: size
 * @_stdin: file
 *
 * Return: depend
*/

ssize_t custom_getline(char **command_buff, size_t *n, FILE *_stdin)
{
	return (custom_getline(command_buff, n, _stdin));
}
