#include "simple.h"

/**
 * custom_strtok - strtok
 * @string: str
 * @delim: delim
 *
 * Return: pointer or NULL
*/

char *custom_strtok(char *string, const char *delim)
{
	return (custom_strtok(string, delim));
}
