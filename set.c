#include "simple.h"
#include <stdlib.h>

/**
 * custom_setenv - env variables
 * @name: name
 * @value: value
 * @overwrite: overwrite
 *
 * Return: int or -1
*/

int custom_setenv(const char *name, const char *value, int overwrite)
{
	return (custom_setenv(name, value, overwrite));
}
